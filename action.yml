name: 'Hugo in Action'
description: 'Build Hugo extended site, Deploy to GitHub Page'
branding:
  icon: 'award'  
  color: 'green'
inputs:
  branch: # id of input
    description: 'which branch will publish'
    required: true
    default: 'master'
runs:
  using: 'docker'
  image: 'docker://wxdlong/hugo'